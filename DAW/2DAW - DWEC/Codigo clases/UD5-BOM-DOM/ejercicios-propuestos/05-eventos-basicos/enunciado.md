# Ejercicio 5 – Eventos básicos
1. Crea tres botones que muestren distintos mensajes en consola.
2. Usa `addEventListener()` (no `onclick`).
3. Campo `<input>` que muestre su contenido en tiempo real con el evento `input`.
4. Prueba `mouseenter` y `mouseleave` sobre un div coloreado.
