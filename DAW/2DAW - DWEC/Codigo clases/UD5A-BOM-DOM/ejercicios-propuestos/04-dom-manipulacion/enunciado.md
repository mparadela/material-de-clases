# Ejercicio 4 – Manipulación del DOM
1. Crea un `<ul>` vacío y un botón “Añadir elemento”.
2. Cada clic añade un `<li>` numerado.
3. Otro botón “Eliminar último elemento” borra el último nodo.
4. Implementa `removeChild()` o `remove()`.
